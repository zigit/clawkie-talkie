# Repo-root env for Clawkie-Talkie local dev.
#
# Copy to `.env` and fill in values. The root daemon scripts load this file
# with `tsx --env-file=.env`, so `npm run dev` and `npm run daemon` pick up
# values automatically.
#
# `.env` is gitignored — never commit your real key.

XAI_API_KEY=

# Signaling server URL (rambly-style SSE+POST). The daemon and the
# browser must agree on this. Defaults to https://api.rambly.app.
# For local dev, run `npm run signal` and set both:
#   SIGNAL_SERVER=http://localhost:8787
#   VITE_SIGNAL_SERVER=http://localhost:8787
# (VITE_-prefixed vars are inlined into the client bundle by Vite.)
SIGNAL_SERVER=
VITE_SIGNAL_SERVER=
